PROTOBUF_GENERATE_CPP(PROTO_SRCS PROTO_HDRS echo.proto)
include_directories(${CMAKE_CURRENT_BINARY_DIR})
set(SRCS
    echo_impl.cc
    ${PROTO_SRCS})
add_executable(echo_server server.cc ${SRCS})
add_executable(echo_client client.cc ${SRCS})
target_link_libraries(echo_server lao_rpc)
target_link_libraries(echo_client lao_rpc)